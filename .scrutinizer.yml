build:
  tests:
    override:
      -
        command: 'phpunit -c phpunit.xml --coverage-clover=./build/logs/clover.xml'
        coverage:
          file: './build/logs/clover.xml'
          format: 'php-clover'
